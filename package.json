{
  "name": "voice-text-search",
  "version": "1.0.0",
  "scripts": {
    "build": "npx tsc && cp -r src/types.ts dist/",
    "watch": "tsc -w"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.259",
    "@types/dom-speech-recognition": "^0.0.3",
    "typescript": "^5.7.3"
  }
}
